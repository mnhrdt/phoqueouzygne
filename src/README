to test, you only need the raw.dat image

./extract_img raw.dat 3000 4000 -x x.npy -z z.npy

creates the following two complex images, corresponding to records 3000-4000

x.npy : raw data (decoded)
z.npy : horizontal focused raw data



to extract the whole medatada series (one datum per record):

./dump_headers img.dat annot.dat index.dat dummy > metadata.txt
