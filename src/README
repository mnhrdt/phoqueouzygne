to test, you only need the raw.dat image

./extract_img raw.dat 3000 4000

creates the following images, corresponding to records 3000-4000

x_real.tiff  # raw data, real part
x_imag.tiff  # raw data, imaginary part
x_norm.tiff  # raw data, norm
y_norm.tiff  # focused data, norm
